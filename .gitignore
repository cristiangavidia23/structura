# Xcode
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
DerivedData/
.build/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# xcodegen: project.pbxproj is generated from project.yml, but we keep it
# checked in so the repo can be opened without running xcodegen first.

# Swift Package Manager
.swiftpm/

# macOS
.DS_Store

# Fastlane — secretos y artefactos locales. `fastlane/.env.default`,
# `fastlane/Appfile` y `fastlane/Fastfile` sí se commitean (no tienen
# secretos); todo lo de acá abajo es por-máquina o generado en cada corrida.
fastlane/.env
fastlane/report.xml
fastlane/test_output/
fastlane/README.md
build/

# Bundler
.bundle/
vendor/bundle/
